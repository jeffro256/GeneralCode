import sys

if len(sys.argv) >= 2:
    num = int(sys.argv[1])
else:
    print('Missing argument for number of triangle numbers.')
    exit()

for n in range(1, num + 1):
    print(n, (n*(n+1))//2, sep='\t')
